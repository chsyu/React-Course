const ProfileCard = ({ redirect }) => {

  return (
    <h1 style={{
      display: 'flex',
      height: '100%',
      justifyContent: 'center',
      alignItems: 'center',
    }}> profile page ...</h1>
  );
};
export default ProfileCard;
